package example.flowpoints;

import org.evidently.agent.ASTType;
import org.evidently.agent.Flowpoint;

public class Flowpoint2  {

	//
	// this flowpoint should get a field called f (but specifically from FlowpointCollector) and only if it is within 
	// the execution of the doStuff method inside SomeClass
	//

//	public String getFlowpointFor(String scope, String name, ASTType type) {
//		
//		if(type==ASTType.FIELD)
//			return null;
//		}
//
}
